{
  "builds": [
    {
      "src": "api/**/*.ts",
      "dist": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "dist": "/(.*)",
      "destination": "/api/index.ts"
    }
  ]
}
